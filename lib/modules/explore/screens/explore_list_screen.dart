import 'package:flutter/material.dart';

class ExploreListScreen extends StatelessWidget {
  const ExploreListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
